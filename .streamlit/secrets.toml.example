# Copy to .streamlit/secrets.toml for local runs, or paste the same line into
# the hosting provider's Secrets box. NEVER commit the real secrets.toml -
# .gitignore already excludes it.

app_password = "choose-a-strong-shared-password"
